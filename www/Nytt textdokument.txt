CREATE TABLE IF NOT EXISTS `products` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `name` varchar(512) NOT NULL,
  `description` text NOT NULL,
  `price` float NOT NULL,
  PRIMARY KEY (`id`)
) AUTO_INCREMENT=20 ;


INSERT INTO `products` (`id`, `name`, `description`, `price`) VALUES
(1, 'Forsen:\nSnus Brotherhood Hoodie.', 'Snus brotherhood Hoodie: The official Snus brotherhood Hoodie comes in a variety of different colors such as: Black, White, Blue, Red, Purple, Orange and Green.', 19.99); 


INSERT INTO `products` (`id`, `name`, `description`, `price`) VALUES
(2, 'Forsen:\nThug life T-shirt.', 'Thug life T-shirt: Forsen's own thug life t-shirt portraits the best about Forsen, in this shirt you will find well-known memes such as: "Kappa", "TryHard", the cigarette from montage parodies and of course the thug life meme.', 15.99);


CREATE TABLE `forum_question` (
`id` int(4) NOT NULL auto_increment,
`topic` varchar(255) NOT NULL default '',
`detail` longtext NOT NULL,
`name` varchar(65) NOT NULL default '',
`email` varchar(65) NOT NULL default '',
`datetime` varchar(25) NOT NULL default '',
`view` int(4) NOT NULL default '0',
`reply` int(4) NOT NULL default '0',
PRIMARY KEY (`id`)
) AUTO_INCREMENT=1;


<script>

	$cart = []; // tom lista
	
	function buyProduct(n,name){
		$length = $cart.length;
		
		$cart[$length] = [];    // lägg en tom lista i lisan (inception)    [ [10,"Kaluha"],[9,"Sleepy"],[3,"Fish"] ]
		
		
		$cart[$length][0] = n;    //[[x,0]]
		$cart[$length][1] = name; //[[0,x]]
		
		document.getElementById('cart').innerHTML = "";
		
		for($i=0;$i<$length+1;$i++){
			document.getElementById('cart').innerHTML += $cart[$i][1] + "<br>";
			
		}
	
	}
	</script>
	
	<div id="cart">
	
	</div>